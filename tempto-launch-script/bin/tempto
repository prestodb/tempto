#!/bin/sh

READLINK=readlink
if [[ `uname` == "Darwin" ]]; then
  READLINK=greadlink
fi

SCRIPT_ROOT=$($READLINK -f $(dirname $($READLINK -f "$0"))/..)

exec python $SCRIPT_ROOT/runner/test_runner.py "$@"
